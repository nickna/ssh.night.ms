# Binaries
/nightms
/nightms.exe
/cmd/**/dist/
/bin/
/*.exe

# Host keys (generated per-environment)
/data/host-keys/
/data/

# Go workspace files
/vendor/
go.work
go.work.sum

# Editor / OS
.vscode/
.idea/
.claude/
*.swp
*.swo
.DS_Store
Thumbs.db

# Production secrets — the committed .env.example is the template; never the filled-in .env
deploy/.env
